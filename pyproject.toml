[project]
name = "sports-commentary-engine"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["config*", "ingestion*", "llm*", "api*", "dashboard*", "scripts*"]
